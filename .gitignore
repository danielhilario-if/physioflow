.venv/
__pycache__/
*.pyc
.DS_Store
.streamlit/secrets.toml

# Testes e Cobertura
.coverage
coverage.xml
htmlcov/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# Artefatos gerados pelo build do manual (PDFs locais; o CI publica como artifact)
docs/manual.*.pdf

# Memória interna do Claude Code — não vai para o repo
.claude-memory/

# Material de referência (PDFs/artigos pesados; não versionar)
artigos_base/
