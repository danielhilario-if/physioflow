streamlit>=1.40
streamlit-option-menu>=0.4.0
pandas>=2.0
numpy>=1.24
seaborn>=0.13
matplotlib>=3.7
scikit-learn>=1.3
openpyxl>=3.1
supabase>=2.8
scipy>=1.11
statsmodels>=0.14
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
libpysal>=4.9
esda>=2.5
# geobr foi removido para o deploy no Streamlit Cloud porque ele puxa lxml
# como dependência transitiva e, em Python 3.14 (versão atualmente usada pelo
# Cloud), não há wheel pré-compilado de lxml — o pip tenta compilar do código
# e exige libxml2-dev/libxslt-dev do sistema, que o Cloud não disponibiliza.
# Reinstale localmente com `pip install geobr>=0.2.2` se quiser o overlay do
# município na aba "Mapa sobre Rio Verde" da página Análise Espacial. As
# outras 5 abas espaciais e as 7 demais páginas funcionam sem geobr.
#
# pytest foi removido — é dependência de desenvolvimento, mantida em
# requirements-dev.txt.
